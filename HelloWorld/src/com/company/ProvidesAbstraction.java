package com.company;

public interface ProvidesAbstraction {

    // Open Closed principle because it is open for extension like addition , subtraction......

    public void operationM(double a,double b);
}
